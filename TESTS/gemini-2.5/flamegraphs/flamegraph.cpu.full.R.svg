<?xml version="1.0" standalone="no"?>
<!DOCTYPE svg PUBLIC "-//W3C//DTD SVG 1.1//EN" "http://www.w3.org/Graphics/SVG/1.1/DTD/svg11.dtd">
<svg version="1.1" width="4096" height="252" onload="init(evt)" viewBox="0 0 4096 252" xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink">
<!-- Flame graph stack visualization. See https://github.com/brendangregg/FlameGraph for latest version, and http://www.brendangregg.com/flamegraphs.html for examples. -->
<!-- NOTES:  -->
<defs>
        <linearGradient id="background" y1="0" y2="1" x1="0" x2="0" >
                <stop stop-color="#f8f8f8" offset="5%" />
                <stop stop-color="#e8e8e8" offset="95%" />
        </linearGradient>
</defs>
<style type="text/css">
        text { font-family:Verdana; font-size:10px; fill:rgb(0,0,0); }
        .hide { display:none; }
        .parent { opacity:0.5; }
</style>
<script type="text/ecmascript">
<![CDATA[
        "use strict";
        var details, searchbtn, unzoombtn, matchedtxt, svg, searching, currentSearchTerm, ignorecase, ignorecaseBtn;
        function init(evt) {
                details = document.getElementById("details").firstChild;
                searchbtn = document.getElementById("search");
                ignorecaseBtn = document.getElementById("ignorecase");
                unzoombtn = document.getElementById("unzoom");
                matchedtxt = document.getElementById("matched");
                svg = document.getElementsByTagName("svg")[0];
                searching = 0;
                currentSearchTerm = null;

                // use GET parameters to restore a flamegraphs state.
                var params = get_params();
                if (params.x && params.y)
                        zoom(find_group(document.querySelector('[x="' + params.x + '"][y="' + params.y + '"]')));
                if (params.s) search(params.s);
        }

        // event listeners
        window.addEventListener("click", function(e) {
                var target = find_group(e.target);
                if (target) {
                        if (target.nodeName == "a") {
                                if (e.ctrlKey === false) return;
                                e.preventDefault();
                        }
                        if (target.classList.contains("parent")) unzoom(true);
                        zoom(target);
                        if (!document.querySelector('.parent')) {
                                // we have basically done a clearzoom so clear the url
                                var params = get_params();
                                if (params.x) delete params.x;
                                if (params.y) delete params.y;
                                history.replaceState(null, null, parse_params(params));
                                unzoombtn.classList.add("hide");
                                return;
                        }

                        // set parameters for zoom state
                        var el = target.querySelector("rect");
                        if (el && el.attributes && el.attributes.y && el.attributes._orig_x) {
                                var params = get_params()
                                params.x = el.attributes._orig_x.value;
                                params.y = el.attributes.y.value;
                                history.replaceState(null, null, parse_params(params));
                        }
                }
                else if (e.target.id == "unzoom") clearzoom();
                else if (e.target.id == "search") search_prompt();
                else if (e.target.id == "ignorecase") toggle_ignorecase();
        }, false)

        // mouse-over for info
        // show
        window.addEventListener("mouseover", function(e) {
                var target = find_group(e.target);
                if (target) details.nodeValue = "Function: " + g_to_text(target);
        }, false)

        // clear
        window.addEventListener("mouseout", function(e) {
                var target = find_group(e.target);
                if (target) details.nodeValue = ' ';
        }, false)

        // ctrl-F for search
        // ctrl-I to toggle case-sensitive search
        window.addEventListener("keydown",function (e) {
                if (e.keyCode === 114 || (e.ctrlKey && e.keyCode === 70)) {
                        e.preventDefault();
                        search_prompt();
                }
                else if (e.ctrlKey && e.keyCode === 73) {
                        e.preventDefault();
                        toggle_ignorecase();
                }
        }, false)

        // functions
        function get_params() {
                var params = {};
                var paramsarr = window.location.search.substr(1).split('&');
                for (var i = 0; i < paramsarr.length; ++i) {
                        var tmp = paramsarr[i].split("=");
                        if (!tmp[0] || !tmp[1]) continue;
                        params[tmp[0]]  = decodeURIComponent(tmp[1]);
                }
                return params;
        }
        function parse_params(params) {
                var uri = "?";
                for (var key in params) {
                        uri += key + '=' + encodeURIComponent(params[key]) + '&';
                }
                if (uri.slice(-1) == "&")
                        uri = uri.substring(0, uri.length - 1);
                if (uri == '?')
                        uri = window.location.href.split('?')[0];
                return uri;
        }
        function find_child(node, selector) {
                var children = node.querySelectorAll(selector);
                if (children.length) return children[0];
        }
        function find_group(node) {
                var parent = node.parentElement;
                if (!parent) return;
                if (parent.id == "frames") return node;
                return find_group(parent);
        }
        function orig_save(e, attr, val) {
                if (e.attributes["_orig_" + attr] != undefined) return;
                if (e.attributes[attr] == undefined) return;
                if (val == undefined) val = e.attributes[attr].value;
                e.setAttribute("_orig_" + attr, val);
        }
        function orig_load(e, attr) {
                if (e.attributes["_orig_"+attr] == undefined) return;
                e.attributes[attr].value = e.attributes["_orig_" + attr].value;
                e.removeAttribute("_orig_"+attr);
        }
        function g_to_text(e) {
                var text = find_child(e, "title").firstChild.nodeValue;
                return (text)
        }
        function g_to_func(e) {
                var func = g_to_text(e);
                // if there's any manipulation we want to do to the function
                // name before it's searched, do it here before returning.
                return (func);
        }
        function update_text(e) {
                var r = find_child(e, "rect");
                var t = find_child(e, "text");
                var w = parseFloat(r.attributes.width.value) -3;
                var txt = find_child(e, "title").textContent.replace(/\([^(]*\)$/,"");
                t.attributes.x.value = parseFloat(r.attributes.x.value) + 3;

                // Smaller than this size won't fit anything
                if (w < 2 * 10 * 0.59) {
                        t.textContent = "";
                        return;
                }

                t.textContent = txt;
                var sl = t.getSubStringLength(0, txt.length);
                // check if only whitespace or if we can fit the entire string into width w
                if (/^ *$/.test(txt) || sl < w)
                        return;

                // this isn't perfect, but gives a good starting point
                // and avoids calling getSubStringLength too often
                var start = Math.floor((w/sl) * txt.length);
                for (var x = start; x > 0; x = x-2) {
                        if (t.getSubStringLength(0, x + 2) <= w) {
                                t.textContent = txt.substring(0, x) + "..";
                                return;
                        }
                }
                t.textContent = "";
        }

        // zoom
        function zoom_reset(e) {
                if (e.attributes != undefined) {
                        orig_load(e, "x");
                        orig_load(e, "width");
                }
                if (e.childNodes == undefined) return;
                for (var i = 0, c = e.childNodes; i < c.length; i++) {
                        zoom_reset(c[i]);
                }
        }
        function zoom_child(e, x, ratio) {
                if (e.attributes != undefined) {
                        if (e.attributes.x != undefined) {
                                orig_save(e, "x");
                                e.attributes.x.value = (parseFloat(e.attributes.x.value) - x - 10) * ratio + 10;
                                if (e.tagName == "text")
                                        e.attributes.x.value = find_child(e.parentNode, "rect[x]").attributes.x.value + 3;
                        }
                        if (e.attributes.width != undefined) {
                                orig_save(e, "width");
                                e.attributes.width.value = parseFloat(e.attributes.width.value) * ratio;
                        }
                }

                if (e.childNodes == undefined) return;
                for (var i = 0, c = e.childNodes; i < c.length; i++) {
                        zoom_child(c[i], x - 10, ratio);
                }
        }
        function zoom_parent(e) {
                if (e.attributes) {
                        if (e.attributes.x != undefined) {
                                orig_save(e, "x");
                                e.attributes.x.value = 10;
                        }
                        if (e.attributes.width != undefined) {
                                orig_save(e, "width");
                                e.attributes.width.value = parseInt(svg.width.baseVal.value) - (10 * 2);
                        }
                }
                if (e.childNodes == undefined) return;
                for (var i = 0, c = e.childNodes; i < c.length; i++) {
                        zoom_parent(c[i]);
                }
        }
        function zoom(node) {
                var attr = find_child(node, "rect").attributes;
                var width = parseFloat(attr.width.value);
                var xmin = parseFloat(attr.x.value);
                var xmax = parseFloat(xmin + width);
                var ymin = parseFloat(attr.y.value);
                var ratio = (svg.width.baseVal.value - 2 * 10) / width;

                // XXX: Workaround for JavaScript float issues (fix me)
                var fudge = 0.0001;

                unzoombtn.classList.remove("hide");

                var el = document.getElementById("frames").children;
                for (var i = 0; i < el.length; i++) {
                        var e = el[i];
                        var a = find_child(e, "rect").attributes;
                        var ex = parseFloat(a.x.value);
                        var ew = parseFloat(a.width.value);
                        var upstack;
                        // Is it an ancestor
                        if (1 == 0) {
                                upstack = parseFloat(a.y.value) > ymin;
                        } else {
                                upstack = parseFloat(a.y.value) < ymin;
                        }
                        if (upstack) {
                                // Direct ancestor
                                if (ex <= xmin && (ex+ew+fudge) >= xmax) {
                                        e.classList.add("parent");
                                        zoom_parent(e);
                                        update_text(e);
                                }
                                // not in current path
                                else
                                        e.classList.add("hide");
                        }
                        // Children maybe
                        else {
                                // no common path
                                if (ex < xmin || ex + fudge >= xmax) {
                                        e.classList.add("hide");
                                }
                                else {
                                        zoom_child(e, xmin, ratio);
                                        update_text(e);
                                }
                        }
                }
                search();
        }
        function unzoom(dont_update_text) {
                unzoombtn.classList.add("hide");
                var el = document.getElementById("frames").children;
                for(var i = 0; i < el.length; i++) {
                        el[i].classList.remove("parent");
                        el[i].classList.remove("hide");
                        zoom_reset(el[i]);
                        if(!dont_update_text) update_text(el[i]);
                }
                search();
        }
        function clearzoom() {
                unzoom();

                // remove zoom state
                var params = get_params();
                if (params.x) delete params.x;
                if (params.y) delete params.y;
                history.replaceState(null, null, parse_params(params));
        }

        // search
        function toggle_ignorecase() {
                ignorecase = !ignorecase;
                if (ignorecase) {
                        ignorecaseBtn.classList.add("show");
                } else {
                        ignorecaseBtn.classList.remove("show");
                }
                reset_search();
                search();
        }
        function reset_search() {
                var el = document.querySelectorAll("#frames rect");
                for (var i = 0; i < el.length; i++) {
                        orig_load(el[i], "fill")
                }
                var params = get_params();
                delete params.s;
                history.replaceState(null, null, parse_params(params));
        }
        function search_prompt() {
                if (!searching) {
                        var term = prompt("Enter a search term (regexp " +
                            "allowed, eg: ^ext4_)"
                            + (ignorecase ? ", ignoring case" : "")
                            + "\nPress Ctrl-i to toggle case sensitivity", "");
                        if (term != null) search(term);
                } else {
                        reset_search();
                        searching = 0;
                        currentSearchTerm = null;
                        searchbtn.classList.remove("show");
                        searchbtn.firstChild.nodeValue = "Search"
                        matchedtxt.classList.add("hide");
                        matchedtxt.firstChild.nodeValue = ""
                }
        }
        function search(term) {
                if (term) currentSearchTerm = term;
                if (currentSearchTerm === null) return;

                var re = new RegExp(currentSearchTerm, ignorecase ? 'i' : '');
                var el = document.getElementById("frames").children;
                var matches = new Object();
                var maxwidth = 0;
                for (var i = 0; i < el.length; i++) {
                        var e = el[i];
                        var func = g_to_func(e);
                        var rect = find_child(e, "rect");
                        if (func == null || rect == null)
                                continue;

                        // Save max width. Only works as we have a root frame
                        var w = parseFloat(rect.attributes.width.value);
                        if (w > maxwidth)
                                maxwidth = w;

                        if (func.match(re)) {
                                // highlight
                                var x = parseFloat(rect.attributes.x.value);
                                orig_save(rect, "fill");
                                rect.attributes.fill.value = "rgb(230,0,230)";

                                // remember matches
                                if (matches[x] == undefined) {
                                        matches[x] = w;
                                } else {
                                        if (w > matches[x]) {
                                                // overwrite with parent
                                                matches[x] = w;
                                        }
                                }
                                searching = 1;
                        }
                }
                if (!searching)
                        return;
                var params = get_params();
                params.s = currentSearchTerm;
                history.replaceState(null, null, parse_params(params));

                searchbtn.classList.add("show");
                searchbtn.firstChild.nodeValue = "Reset Search";

                // calculate percent matched, excluding vertical overlap
                var count = 0;
                var lastx = -1;
                var lastw = 0;
                var keys = Array();
                for (k in matches) {
                        if (matches.hasOwnProperty(k))
                                keys.push(k);
                }
                // sort the matched frames by their x location
                // ascending, then width descending
                keys.sort(function(a, b){
                        return a - b;
                });
                // Step through frames saving only the biggest bottom-up frames
                // thanks to the sort order. This relies on the tree property
                // where children are always smaller than their parents.
                var fudge = 0.0001;     // JavaScript floating point
                for (var k in keys) {
                        var x = parseFloat(keys[k]);
                        var w = matches[keys[k]];
                        if (x >= lastx + lastw - fudge) {
                                count += w;
                                lastx = x;
                                lastw = w;
                        }
                }
                // display matched percent
                matchedtxt.classList.remove("hide");
                var pct = 100 * count / maxwidth;
                if (pct != 100) pct = pct.toFixed(1)
                matchedtxt.firstChild.nodeValue = "Matched: " + pct + "%";
        }
]]>
</script>
<rect x="0.0" y="0" width="4096.0" height="252.0" fill="url(#background)"  />
<text id="title" x="2048.00" y="20" >FlameGraph: /mnt/ramdisk/timep/TESTS/gemini-2.5/gemini-2.5.test.bash</text>
<text id="details" x="10.00" y="237" > </text>
<text id="unzoom" x="10.00" y="20" class="hide">Reset Zoom</text>
<text id="search" x="3986.00" y="20" >Search</text>
<text id="ignorecase" x="4070.00" y="20" >ic</text>
<text id="matched" x="3986.00" y="237" > </text>
<g id="frames">
<g >
<title>'local depth=$1' (84 us, 0.01%)</title><rect x="4048.2" y="126" width="0.4" height="23.0" fill="rgb(10,28,252)" rx="2" ry="2" />
<text  x="4051.18" y="140.5" ></text>
</g>
<g >
<title>SUBSHELL (964917)_[s] (8,063 us, 0.94%)</title><rect x="4047.8" y="78" width="38.2" height="23.0" fill="rgb(225.780287272499,148.746499172724,201.873249586362)" rx="2" ry="2" />
<text  x="4050.79" y="92.5" >SUBS..</text>
</g>
<g >
<title>'read -r line' (205 us, 0.02%)</title><rect x="4044.9" y="102" width="0.9" height="23.0" fill="rgb(10,28,252)" rx="2" ry="2" />
<text  x="4047.86" y="116.5" ></text>
</g>
<g >
<title>'OUTPUT_PIPE=&quot;$TMPDIR/output.pipe&quot;' (79 us, 0.01%)</title><rect x="2021.8" y="78" width="0.3" height="23.0" fill="rgb(10,28,252)" rx="2" ry="2" />
<text  x="2024.76" y="92.5" ></text>
</g>
<g >
<title>'mktemp -d' (424,107 us, 49.30%)</title><rect x="2033.8" y="102" width="2009.6" height="23.0" fill="rgb(252,28,10)" rx="2" ry="2" />
<text  x="2036.77" y="116.5" >'mktemp -d'</text>
</g>
<g >
<title>'echo &quot;[COPROC] Received: $line&quot; 1&gt;&amp;2' (99 us, 0.01%)</title><rect x="4044.0" y="102" width="0.5" height="23.0" fill="rgb(10,28,252)" rx="2" ry="2" />
<text  x="4046.99" y="116.5" ></text>
</g>
<g >
<title>SUBSHELL (964909)_[s] (424,107 us, 49.30%)</title><rect x="2033.8" y="78" width="2009.6" height="23.0" fill="rgb(255,155.169020426792,229.329176681175)" rx="2" ry="2" />
<text  x="2036.77" y="92.5" >SUBSHELL (964909)_[s]</text>
</g>
<g >
<title>FUNCTION (recursive_function)_[f] (606 us, 0.07%)</title><rect x="4083.1" y="174" width="2.9" height="23.0" fill="rgb(199.911868690286,144.823737380572,224.701527779657)" rx="2" ry="2" />
<text  x="4086.13" y="188.5" ></text>
</g>
<g >
<title>SUBSHELL (964912)_[s] (926 us, 0.11%)</title><rect x="4043.4" y="78" width="4.4" height="23.0" fill="rgb(225.160233647759,146.491758719123,200.745879359562)" rx="2" ry="2" />
<text  x="4046.41" y="92.5" ></text>
</g>
<g >
<title>'local depth=$1' (83 us, 0.01%)</title><rect x="4056.0" y="150" width="0.4" height="23.0" fill="rgb(10,28,252)" rx="2" ry="2" />
<text  x="4058.96" y="164.5" ></text>
</g>
<g >
<title>'recursive_function 1' (82 us, 0.01%)</title><rect x="4047.8" y="126" width="0.4" height="23.0" fill="rgb(10,28,252)" rx="2" ry="2" />
<text  x="4050.79" y="140.5" ></text>
</g>
<g >
<title>'sleep 0.1' (2,579 us, 0.30%)</title><rect x="4070.9" y="174" width="12.2" height="23.0" fill="rgb(10,28,252)" rx="2" ry="2" />
<text  x="4073.91" y="188.5" ></text>
</g>
<g >
<title>'echo &quot;$line&quot;' (123 us, 0.01%)</title><rect x="4046.5" y="102" width="0.6" height="23.0" fill="rgb(10,28,252)" rx="2" ry="2" />
<text  x="4049.54" y="116.5" ></text>
</g>
<g >
<title>'echo &quot;[COPROC] Received: $line&quot; 1&gt;&amp;2' (149 us, 0.02%)</title><rect x="4045.8" y="102" width="0.7" height="23.0" fill="rgb(10,28,252)" rx="2" ry="2" />
<text  x="4048.84" y="116.5" ></text>
</g>
<g >
<title>FUNCTION (recursive_function)_[f] (8,063 us, 0.94%)</title><rect x="4047.8" y="102" width="38.2" height="23.0" fill="rgb(201.873249586362,148.746499172724,225.780287272499)" rx="2" ry="2" />
<text  x="4050.79" y="116.5" >FUNC..</text>
</g>
<g >
<title>'[ $depth -gt 3 ]' (119 us, 0.01%)</title><rect x="4069.9" y="174" width="0.6" height="23.0" fill="rgb(10,28,252)" rx="2" ry="2" />
<text  x="4072.89" y="188.5" ></text>
</g>
<g >
<title>'echo &quot;[RECURSIVE] Depth: $depth&quot; 1&gt;&amp;2' (90 us, 0.01%)</title><rect x="4049.0" y="126" width="0.4" height="23.0" fill="rgb(10,28,252)" rx="2" ry="2" />
<text  x="4051.98" y="140.5" ></text>
</g>
<g >
<title>'mkfifo &quot;$OUTPUT_PIPE&quot;' (1,068 us, 0.12%)</title><rect x="2028.7" y="78" width="5.1" height="23.0" fill="rgb(10,28,252)" rx="2" ry="2" />
<text  x="2031.71" y="92.5" ></text>
</g>
<g >
<title>'echo &quot;[RECURSIVE] Depth: $depth&quot; 1&gt;&amp;2' (94 us, 0.01%)</title><rect x="4056.7" y="150" width="0.5" height="23.0" fill="rgb(10,28,252)" rx="2" ry="2" />
<text  x="4059.74" y="164.5" ></text>
</g>
<g >
<title>FUNCTION (recursive_function)_[f] (6,488 us, 0.75%)</title><rect x="4055.3" y="126" width="30.7" height="23.0" fill="rgb(201.737360057582,148.474720115165,225.70554803167)" rx="2" ry="2" />
<text  x="4058.26" y="140.5" >FUN..</text>
</g>
<g >
<title>SUBSHELL (964884)_[s] (860,186 us, 100.00%)</title><rect x="10.0" y="54" width="4076.0" height="23.0" fill="rgb(225.842659417414,148.973306972413,201.986653486207)" rx="2" ry="2" />
<text  x="13.00" y="68.5" >SUBSHELL (964884)_[s]</text>
</g>
<g >
<title>'set -o monitor' (80 us, 0.01%)</title><rect x="10.0" y="78" width="0.4" height="23.0" fill="rgb(10,28,252)" rx="2" ry="2" />
<text  x="13.00" y="92.5" ></text>
</g>
<g >
<title>'[ $depth -gt 3 ]' (159 us, 0.02%)</title><rect x="4084.7" y="198" width="0.7" height="23.0" fill="rgb(10,28,252)" rx="2" ry="2" />
<text  x="4087.67" y="212.5" ></text>
</g>
<g >
<title>'echo &quot;[RECURSIVE] Depth: $depth&quot; 1&gt;&amp;2' (96 us, 0.01%)</title><rect x="4070.5" y="174" width="0.4" height="23.0" fill="rgb(10,28,252)" rx="2" ry="2" />
<text  x="4073.45" y="188.5" ></text>
</g>
<g >
<title>'mkfifo &quot;$INPUT_PIPE&quot;' (1,387 us, 0.16%)</title><rect x="2022.1" y="78" width="6.6" height="23.0" fill="rgb(10,28,252)" rx="2" ry="2" />
<text  x="2025.14" y="92.5" ></text>
</g>
<g >
<title>'TRAP (SIGCHLD): echo &quot;[TRAP] A child process has exited.&quot;' (424,091 us, 49.30%)</title><rect x="11.8" y="78" width="2009.6" height="23.0" fill="rgb(156,197,90)" rx="2" ry="2" />
<text  x="14.82" y="92.5" >'TRAP (SIGCHLD): echo "[TRAP] A child process has exited."'</text>
</g>
<g >
<title>'recursive_function $((depth + 1))' (148 us, 0.02%)</title><rect x="4055.3" y="150" width="0.7" height="23.0" fill="rgb(10,28,252)" rx="2" ry="2" />
<text  x="4058.26" y="164.5" ></text>
</g>
<g >
<title>'[ $depth -gt 3 ]' (85 us, 0.01%)</title><rect x="4048.6" y="126" width="0.4" height="23.0" fill="rgb(10,28,252)" rx="2" ry="2" />
<text  x="4051.58" y="140.5" ></text>
</g>
<g >
<title>all (860,186 us, 100%)</title><rect x="10.0" y="30" width="4076.0" height="23.0" fill="rgb(255,0,0)" rx="2" ry="2" />
<text  x="13.00" y="44.5" ></text>
</g>
<g >
<title>'recursive_function $((depth + 1))' (120 us, 0.01%)</title><rect x="4068.7" y="174" width="0.6" height="23.0" fill="rgb(10,28,252)" rx="2" ry="2" />
<text  x="4071.74" y="188.5" ></text>
</g>
<g >
<title>'TMPDIR=$(mktemp -d)' (303 us, 0.04%)</title><rect x="10.4" y="78" width="1.4" height="23.0" fill="rgb(10,28,252)" rx="2" ry="2" />
<text  x="13.38" y="92.5" ></text>
</g>
<g >
<title>'return' (121 us, 0.01%)</title><rect x="4085.4" y="198" width="0.6" height="23.0" fill="rgb(10,28,252)" rx="2" ry="2" />
<text  x="4088.43" y="212.5" ></text>
</g>
<g >
<title>'INPUT_PIPE=&quot;$TMPDIR/input.pipe&quot;' (81 us, 0.01%)</title><rect x="2021.4" y="78" width="0.4" height="23.0" fill="rgb(10,28,252)" rx="2" ry="2" />
<text  x="2024.38" y="92.5" ></text>
</g>
<g >
<title>'sleep 0.1' (2,437 us, 0.28%)</title><rect x="4057.2" y="150" width="11.5" height="23.0" fill="rgb(10,28,252)" rx="2" ry="2" />
<text  x="4060.19" y="164.5" ></text>
</g>
<g >
<title>'sleep 0.1' (1,234 us, 0.14%)</title><rect x="4049.4" y="126" width="5.9" height="23.0" fill="rgb(10,28,252)" rx="2" ry="2" />
<text  x="4052.41" y="140.5" ></text>
</g>
<g >
<title>'read -r line' (141 us, 0.02%)</title><rect x="4047.1" y="102" width="0.7" height="23.0" fill="rgb(10,28,252)" rx="2" ry="2" />
<text  x="4050.13" y="116.5" ></text>
</g>
<g >
<title>'echo &quot;$line&quot;' (85 us, 0.01%)</title><rect x="4044.5" y="102" width="0.4" height="23.0" fill="rgb(10,28,252)" rx="2" ry="2" />
<text  x="4047.46" y="116.5" ></text>
</g>
<g >
<title>FUNCTION (recursive_function)_[f] (3,643 us, 0.42%)</title><rect x="4068.7" y="150" width="17.3" height="23.0" fill="rgb(201.632368206248,148.264736412496,225.647802513436)" rx="2" ry="2" />
<text  x="4071.74" y="164.5" ></text>
</g>
<g >
<title>'local depth=$1' (123 us, 0.01%)</title><rect x="4069.3" y="174" width="0.6" height="23.0" fill="rgb(10,28,252)" rx="2" ry="2" />
<text  x="4072.31" y="188.5" ></text>
</g>
<g >
<title>'[ $depth -gt 3 ]' (83 us, 0.01%)</title><rect x="4056.4" y="150" width="0.3" height="23.0" fill="rgb(10,28,252)" rx="2" ry="2" />
<text  x="4059.35" y="164.5" ></text>
</g>
<g >
<title>'read -r line' (124 us, 0.01%)</title><rect x="4043.4" y="102" width="0.6" height="23.0" fill="rgb(10,28,252)" rx="2" ry="2" />
<text  x="4046.41" y="116.5" ></text>
</g>
<g >
<title>'recursive_function $((depth + 1))' (161 us, 0.02%)</title><rect x="4083.1" y="198" width="0.8" height="23.0" fill="rgb(10,28,252)" rx="2" ry="2" />
<text  x="4086.13" y="212.5" ></text>
</g>
<g >
<title>'local depth=$1' (165 us, 0.02%)</title><rect x="4083.9" y="198" width="0.8" height="23.0" fill="rgb(10,28,252)" rx="2" ry="2" />
<text  x="4086.89" y="212.5" ></text>
</g>
</g>
</svg>
