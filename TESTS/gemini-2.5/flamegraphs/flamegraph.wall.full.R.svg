<?xml version="1.0" standalone="no"?>
<!DOCTYPE svg PUBLIC "-//W3C//DTD SVG 1.1//EN" "http://www.w3.org/Graphics/SVG/1.1/DTD/svg11.dtd">
<svg version="1.1" width="4096" height="252" onload="init(evt)" viewBox="0 0 4096 252" xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink">
<!-- Flame graph stack visualization. See https://github.com/brendangregg/FlameGraph for latest version, and http://www.brendangregg.com/flamegraphs.html for examples. -->
<!-- NOTES:  -->
<defs>
        <linearGradient id="background" y1="0" y2="1" x1="0" x2="0" >
                <stop stop-color="#f8f8f8" offset="5%" />
                <stop stop-color="#e8e8e8" offset="95%" />
        </linearGradient>
</defs>
<style type="text/css">
        text { font-family:Verdana; font-size:10px; fill:rgb(0,0,0); }
        .hide { display:none; }
        .parent { opacity:0.5; }
</style>
<script type="text/ecmascript">
<![CDATA[
        "use strict";
        var details, searchbtn, unzoombtn, matchedtxt, svg, searching, currentSearchTerm, ignorecase, ignorecaseBtn;
        function init(evt) {
                details = document.getElementById("details").firstChild;
                searchbtn = document.getElementById("search");
                ignorecaseBtn = document.getElementById("ignorecase");
                unzoombtn = document.getElementById("unzoom");
                matchedtxt = document.getElementById("matched");
                svg = document.getElementsByTagName("svg")[0];
                searching = 0;
                currentSearchTerm = null;

                // use GET parameters to restore a flamegraphs state.
                var params = get_params();
                if (params.x && params.y)
                        zoom(find_group(document.querySelector('[x="' + params.x + '"][y="' + params.y + '"]')));
                if (params.s) search(params.s);
        }

        // event listeners
        window.addEventListener("click", function(e) {
                var target = find_group(e.target);
                if (target) {
                        if (target.nodeName == "a") {
                                if (e.ctrlKey === false) return;
                                e.preventDefault();
                        }
                        if (target.classList.contains("parent")) unzoom(true);
                        zoom(target);
                        if (!document.querySelector('.parent')) {
                                // we have basically done a clearzoom so clear the url
                                var params = get_params();
                                if (params.x) delete params.x;
                                if (params.y) delete params.y;
                                history.replaceState(null, null, parse_params(params));
                                unzoombtn.classList.add("hide");
                                return;
                        }

                        // set parameters for zoom state
                        var el = target.querySelector("rect");
                        if (el && el.attributes && el.attributes.y && el.attributes._orig_x) {
                                var params = get_params()
                                params.x = el.attributes._orig_x.value;
                                params.y = el.attributes.y.value;
                                history.replaceState(null, null, parse_params(params));
                        }
                }
                else if (e.target.id == "unzoom") clearzoom();
                else if (e.target.id == "search") search_prompt();
                else if (e.target.id == "ignorecase") toggle_ignorecase();
        }, false)

        // mouse-over for info
        // show
        window.addEventListener("mouseover", function(e) {
                var target = find_group(e.target);
                if (target) details.nodeValue = "Function: " + g_to_text(target);
        }, false)

        // clear
        window.addEventListener("mouseout", function(e) {
                var target = find_group(e.target);
                if (target) details.nodeValue = ' ';
        }, false)

        // ctrl-F for search
        // ctrl-I to toggle case-sensitive search
        window.addEventListener("keydown",function (e) {
                if (e.keyCode === 114 || (e.ctrlKey && e.keyCode === 70)) {
                        e.preventDefault();
                        search_prompt();
                }
                else if (e.ctrlKey && e.keyCode === 73) {
                        e.preventDefault();
                        toggle_ignorecase();
                }
        }, false)

        // functions
        function get_params() {
                var params = {};
                var paramsarr = window.location.search.substr(1).split('&');
                for (var i = 0; i < paramsarr.length; ++i) {
                        var tmp = paramsarr[i].split("=");
                        if (!tmp[0] || !tmp[1]) continue;
                        params[tmp[0]]  = decodeURIComponent(tmp[1]);
                }
                return params;
        }
        function parse_params(params) {
                var uri = "?";
                for (var key in params) {
                        uri += key + '=' + encodeURIComponent(params[key]) + '&';
                }
                if (uri.slice(-1) == "&")
                        uri = uri.substring(0, uri.length - 1);
                if (uri == '?')
                        uri = window.location.href.split('?')[0];
                return uri;
        }
        function find_child(node, selector) {
                var children = node.querySelectorAll(selector);
                if (children.length) return children[0];
        }
        function find_group(node) {
                var parent = node.parentElement;
                if (!parent) return;
                if (parent.id == "frames") return node;
                return find_group(parent);
        }
        function orig_save(e, attr, val) {
                if (e.attributes["_orig_" + attr] != undefined) return;
                if (e.attributes[attr] == undefined) return;
                if (val == undefined) val = e.attributes[attr].value;
                e.setAttribute("_orig_" + attr, val);
        }
        function orig_load(e, attr) {
                if (e.attributes["_orig_"+attr] == undefined) return;
                e.attributes[attr].value = e.attributes["_orig_" + attr].value;
                e.removeAttribute("_orig_"+attr);
        }
        function g_to_text(e) {
                var text = find_child(e, "title").firstChild.nodeValue;
                return (text)
        }
        function g_to_func(e) {
                var func = g_to_text(e);
                // if there's any manipulation we want to do to the function
                // name before it's searched, do it here before returning.
                return (func);
        }
        function update_text(e) {
                var r = find_child(e, "rect");
                var t = find_child(e, "text");
                var w = parseFloat(r.attributes.width.value) -3;
                var txt = find_child(e, "title").textContent.replace(/\([^(]*\)$/,"");
                t.attributes.x.value = parseFloat(r.attributes.x.value) + 3;

                // Smaller than this size won't fit anything
                if (w < 2 * 10 * 0.59) {
                        t.textContent = "";
                        return;
                }

                t.textContent = txt;
                var sl = t.getSubStringLength(0, txt.length);
                // check if only whitespace or if we can fit the entire string into width w
                if (/^ *$/.test(txt) || sl < w)
                        return;

                // this isn't perfect, but gives a good starting point
                // and avoids calling getSubStringLength too often
                var start = Math.floor((w/sl) * txt.length);
                for (var x = start; x > 0; x = x-2) {
                        if (t.getSubStringLength(0, x + 2) <= w) {
                                t.textContent = txt.substring(0, x) + "..";
                                return;
                        }
                }
                t.textContent = "";
        }

        // zoom
        function zoom_reset(e) {
                if (e.attributes != undefined) {
                        orig_load(e, "x");
                        orig_load(e, "width");
                }
                if (e.childNodes == undefined) return;
                for (var i = 0, c = e.childNodes; i < c.length; i++) {
                        zoom_reset(c[i]);
                }
        }
        function zoom_child(e, x, ratio) {
                if (e.attributes != undefined) {
                        if (e.attributes.x != undefined) {
                                orig_save(e, "x");
                                e.attributes.x.value = (parseFloat(e.attributes.x.value) - x - 10) * ratio + 10;
                                if (e.tagName == "text")
                                        e.attributes.x.value = find_child(e.parentNode, "rect[x]").attributes.x.value + 3;
                        }
                        if (e.attributes.width != undefined) {
                                orig_save(e, "width");
                                e.attributes.width.value = parseFloat(e.attributes.width.value) * ratio;
                        }
                }

                if (e.childNodes == undefined) return;
                for (var i = 0, c = e.childNodes; i < c.length; i++) {
                        zoom_child(c[i], x - 10, ratio);
                }
        }
        function zoom_parent(e) {
                if (e.attributes) {
                        if (e.attributes.x != undefined) {
                                orig_save(e, "x");
                                e.attributes.x.value = 10;
                        }
                        if (e.attributes.width != undefined) {
                                orig_save(e, "width");
                                e.attributes.width.value = parseInt(svg.width.baseVal.value) - (10 * 2);
                        }
                }
                if (e.childNodes == undefined) return;
                for (var i = 0, c = e.childNodes; i < c.length; i++) {
                        zoom_parent(c[i]);
                }
        }
        function zoom(node) {
                var attr = find_child(node, "rect").attributes;
                var width = parseFloat(attr.width.value);
                var xmin = parseFloat(attr.x.value);
                var xmax = parseFloat(xmin + width);
                var ymin = parseFloat(attr.y.value);
                var ratio = (svg.width.baseVal.value - 2 * 10) / width;

                // XXX: Workaround for JavaScript float issues (fix me)
                var fudge = 0.0001;

                unzoombtn.classList.remove("hide");

                var el = document.getElementById("frames").children;
                for (var i = 0; i < el.length; i++) {
                        var e = el[i];
                        var a = find_child(e, "rect").attributes;
                        var ex = parseFloat(a.x.value);
                        var ew = parseFloat(a.width.value);
                        var upstack;
                        // Is it an ancestor
                        if (1 == 0) {
                                upstack = parseFloat(a.y.value) > ymin;
                        } else {
                                upstack = parseFloat(a.y.value) < ymin;
                        }
                        if (upstack) {
                                // Direct ancestor
                                if (ex <= xmin && (ex+ew+fudge) >= xmax) {
                                        e.classList.add("parent");
                                        zoom_parent(e);
                                        update_text(e);
                                }
                                // not in current path
                                else
                                        e.classList.add("hide");
                        }
                        // Children maybe
                        else {
                                // no common path
                                if (ex < xmin || ex + fudge >= xmax) {
                                        e.classList.add("hide");
                                }
                                else {
                                        zoom_child(e, xmin, ratio);
                                        update_text(e);
                                }
                        }
                }
                search();
        }
        function unzoom(dont_update_text) {
                unzoombtn.classList.add("hide");
                var el = document.getElementById("frames").children;
                for(var i = 0; i < el.length; i++) {
                        el[i].classList.remove("parent");
                        el[i].classList.remove("hide");
                        zoom_reset(el[i]);
                        if(!dont_update_text) update_text(el[i]);
                }
                search();
        }
        function clearzoom() {
                unzoom();

                // remove zoom state
                var params = get_params();
                if (params.x) delete params.x;
                if (params.y) delete params.y;
                history.replaceState(null, null, parse_params(params));
        }

        // search
        function toggle_ignorecase() {
                ignorecase = !ignorecase;
                if (ignorecase) {
                        ignorecaseBtn.classList.add("show");
                } else {
                        ignorecaseBtn.classList.remove("show");
                }
                reset_search();
                search();
        }
        function reset_search() {
                var el = document.querySelectorAll("#frames rect");
                for (var i = 0; i < el.length; i++) {
                        orig_load(el[i], "fill")
                }
                var params = get_params();
                delete params.s;
                history.replaceState(null, null, parse_params(params));
        }
        function search_prompt() {
                if (!searching) {
                        var term = prompt("Enter a search term (regexp " +
                            "allowed, eg: ^ext4_)"
                            + (ignorecase ? ", ignoring case" : "")
                            + "\nPress Ctrl-i to toggle case sensitivity", "");
                        if (term != null) search(term);
                } else {
                        reset_search();
                        searching = 0;
                        currentSearchTerm = null;
                        searchbtn.classList.remove("show");
                        searchbtn.firstChild.nodeValue = "Search"
                        matchedtxt.classList.add("hide");
                        matchedtxt.firstChild.nodeValue = ""
                }
        }
        function search(term) {
                if (term) currentSearchTerm = term;
                if (currentSearchTerm === null) return;

                var re = new RegExp(currentSearchTerm, ignorecase ? 'i' : '');
                var el = document.getElementById("frames").children;
                var matches = new Object();
                var maxwidth = 0;
                for (var i = 0; i < el.length; i++) {
                        var e = el[i];
                        var func = g_to_func(e);
                        var rect = find_child(e, "rect");
                        if (func == null || rect == null)
                                continue;

                        // Save max width. Only works as we have a root frame
                        var w = parseFloat(rect.attributes.width.value);
                        if (w > maxwidth)
                                maxwidth = w;

                        if (func.match(re)) {
                                // highlight
                                var x = parseFloat(rect.attributes.x.value);
                                orig_save(rect, "fill");
                                rect.attributes.fill.value = "rgb(230,0,230)";

                                // remember matches
                                if (matches[x] == undefined) {
                                        matches[x] = w;
                                } else {
                                        if (w > matches[x]) {
                                                // overwrite with parent
                                                matches[x] = w;
                                        }
                                }
                                searching = 1;
                        }
                }
                if (!searching)
                        return;
                var params = get_params();
                params.s = currentSearchTerm;
                history.replaceState(null, null, parse_params(params));

                searchbtn.classList.add("show");
                searchbtn.firstChild.nodeValue = "Reset Search";

                // calculate percent matched, excluding vertical overlap
                var count = 0;
                var lastx = -1;
                var lastw = 0;
                var keys = Array();
                for (k in matches) {
                        if (matches.hasOwnProperty(k))
                                keys.push(k);
                }
                // sort the matched frames by their x location
                // ascending, then width descending
                keys.sort(function(a, b){
                        return a - b;
                });
                // Step through frames saving only the biggest bottom-up frames
                // thanks to the sort order. This relies on the tree property
                // where children are always smaller than their parents.
                var fudge = 0.0001;     // JavaScript floating point
                for (var k in keys) {
                        var x = parseFloat(keys[k]);
                        var w = matches[keys[k]];
                        if (x >= lastx + lastw - fudge) {
                                count += w;
                                lastx = x;
                                lastw = w;
                        }
                }
                // display matched percent
                matchedtxt.classList.remove("hide");
                var pct = 100 * count / maxwidth;
                if (pct != 100) pct = pct.toFixed(1)
                matchedtxt.firstChild.nodeValue = "Matched: " + pct + "%";
        }
]]>
</script>
<rect x="0.0" y="0" width="4096.0" height="252.0" fill="url(#background)"  />
<text id="title" x="2048.00" y="20" >FlameGraph: /mnt/ramdisk/timep/TESTS/gemini-2.5/gemini-2.5.test.bash</text>
<text id="details" x="10.00" y="237" > </text>
<text id="unzoom" x="10.00" y="20" class="hide">Reset Zoom</text>
<text id="search" x="3986.00" y="20" >Search</text>
<text id="ignorecase" x="4070.00" y="20" >ic</text>
<text id="matched" x="3986.00" y="237" > </text>
<g id="frames">
<g >
<title>'echo &quot;$line&quot;' (71 us, 0.00%)</title><rect x="2354.1" y="102" width="0.2" height="23.0" fill="rgb(16,41,251)" rx="2" ry="2" />
<text  x="2357.09" y="116.5" ></text>
</g>
<g >
<title>'local depth=$1' (69 us, 0.00%)</title><rect x="3520.8" y="150" width="0.2" height="23.0" fill="rgb(16,41,251)" rx="2" ry="2" />
<text  x="3523.79" y="164.5" ></text>
</g>
<g >
<title>'recursive_function 1' (68 us, 0.00%)</title><rect x="3242.2" y="126" width="0.2" height="23.0" fill="rgb(16,41,251)" rx="2" ry="2" />
<text  x="3245.20" y="140.5" ></text>
</g>
<g >
<title>'[ $depth -gt 3 ]' (70 us, 0.00%)</title><rect x="3521.0" y="150" width="0.2" height="23.0" fill="rgb(16,41,251)" rx="2" ry="2" />
<text  x="3523.98" y="164.5" ></text>
</g>
<g >
<title>'read -r line' (122 us, 0.01%)</title><rect x="3241.9" y="102" width="0.3" height="23.0" fill="rgb(17,43,251)" rx="2" ry="2" />
<text  x="3244.86" y="116.5" ></text>
</g>
<g >
<title>'sleep 0.1' (101,300 us, 6.81%)</title><rect x="3243.0" y="126" width="277.4" height="23.0" fill="rgb(211,211,212)" rx="2" ry="2" />
<text  x="3245.98" y="140.5" >'sleep 0.1'</text>
</g>
<g >
<title>'read -r line' (4,534 us, 0.30%)</title><rect x="2341.4" y="102" width="12.5" height="23.0" fill="rgb(207,211,212)" rx="2" ry="2" />
<text  x="2344.44" y="116.5" ></text>
</g>
<g >
<title>'echo &quot;[COPROC] Received: $line&quot; 1&gt;&amp;2' (85 us, 0.01%)</title><rect x="2353.9" y="102" width="0.2" height="23.0" fill="rgb(17,43,251)" rx="2" ry="2" />
<text  x="2356.86" y="116.5" ></text>
</g>
<g >
<title>'[ $depth -gt 3 ]' (71 us, 0.00%)</title><rect x="3242.6" y="126" width="0.2" height="23.0" fill="rgb(16,41,251)" rx="2" ry="2" />
<text  x="3245.57" y="140.5" ></text>
</g>
<g >
<title>'[ $depth -gt 3 ]' (100 us, 0.01%)</title><rect x="3802.8" y="174" width="0.3" height="23.0" fill="rgb(16,41,251)" rx="2" ry="2" />
<text  x="3805.83" y="188.5" ></text>
</g>
<g >
<title>'sleep 0.1' (102,546 us, 6.89%)</title><rect x="3521.4" y="150" width="280.9" height="23.0" fill="rgb(208,211,212)" rx="2" ry="2" />
<text  x="3524.39" y="164.5" >'sleep 0.1'</text>
</g>
<g >
<title>'mktemp -d' (424,107 us, 28.50%)</title><rect x="1179.8" y="102" width="1161.6" height="23.0" fill="rgb(225,79,24)" rx="2" ry="2" />
<text  x="1182.79" y="116.5" >'mktemp -d'</text>
</g>
<g >
<title>'echo &quot;[COPROC] Received: $line&quot; 1&gt;&amp;2' (129 us, 0.01%)</title><rect x="3241.2" y="102" width="0.4" height="23.0" fill="rgb(17,43,251)" rx="2" ry="2" />
<text  x="3244.22" y="116.5" ></text>
</g>
<g >
<title>'recursive_function $((depth + 1))' (101 us, 0.01%)</title><rect x="3802.3" y="174" width="0.2" height="23.0" fill="rgb(16,41,251)" rx="2" ry="2" />
<text  x="3805.27" y="188.5" ></text>
</g>
<g >
<title>SUBSHELL (964909)_[s] (424,107 us, 28.50%)</title><rect x="1179.8" y="78" width="1161.6" height="23.0" fill="rgb(250.537763637123,155.157461380634,225.437684095942)" rx="2" ry="2" />
<text  x="1182.79" y="92.5" >SUBSHELL (964909)_[s]</text>
</g>
<g >
<title>'set -o monitor' (66 us, 0.00%)</title><rect x="10.0" y="78" width="0.2" height="23.0" fill="rgb(16,40,251)" rx="2" ry="2" />
<text  x="13.00" y="92.5" ></text>
</g>
<g >
<title>'echo &quot;[RECURSIVE] Depth: $depth&quot; 1&gt;&amp;2' (80 us, 0.01%)</title><rect x="3521.2" y="150" width="0.2" height="23.0" fill="rgb(17,42,251)" rx="2" ry="2" />
<text  x="3524.17" y="164.5" ></text>
</g>
<g >
<title>FUNCTION (recursive_function)_[f] (308,066 us, 20.70%)</title><rect x="3242.2" y="102" width="843.8" height="23.0" fill="rgb(221.666610199471,188.333220398943,236.666635609709)" rx="2" ry="2" />
<text  x="3245.20" y="116.5" >FUNCTION (recursive_function)_[f]</text>
</g>
<g >
<title>'sleep 0.1' (102,680 us, 6.90%)</title><rect x="3803.3" y="174" width="281.3" height="23.0" fill="rgb(208,211,212)" rx="2" ry="2" />
<text  x="3806.33" y="188.5" >'sleep 0.1'</text>
</g>
<g >
<title>'[ $depth -gt 3 ]' (135 us, 0.01%)</title><rect x="4085.3" y="198" width="0.4" height="23.0" fill="rgb(17,42,251)" rx="2" ry="2" />
<text  x="4088.35" y="212.5" ></text>
</g>
<g >
<title>'TRAP (SIGCHLD): echo &quot;[TRAP] A child process has exited.&quot;' (424,091 us, 28.50%)</title><rect x="11.5" y="78" width="1161.6" height="23.0" fill="rgb(145,206,120)" rx="2" ry="2" />
<text  x="14.49" y="92.5" >'TRAP (SIGCHLD): echo "[TRAP] A child process has exited."'</text>
</g>
<g >
<title>'local depth=$1' (104 us, 0.01%)</title><rect x="3802.5" y="174" width="0.3" height="23.0" fill="rgb(16,42,251)" rx="2" ry="2" />
<text  x="3805.55" y="188.5" ></text>
</g>
<g >
<title>'OUTPUT_PIPE=&quot;$TMPDIR/output.pipe&quot;' (67 us, 0.00%)</title><rect x="1173.3" y="78" width="0.2" height="23.0" fill="rgb(17,42,251)" rx="2" ry="2" />
<text  x="1176.28" y="92.5" ></text>
</g>
<g >
<title>'mkfifo &quot;$OUTPUT_PIPE&quot;' (997 us, 0.07%)</title><rect x="1177.1" y="78" width="2.7" height="23.0" fill="rgb(19,48,251)" rx="2" ry="2" />
<text  x="1180.06" y="92.5" ></text>
</g>
<g >
<title>'return' (103 us, 0.01%)</title><rect x="4085.7" y="198" width="0.3" height="23.0" fill="rgb(17,42,251)" rx="2" ry="2" />
<text  x="4088.72" y="212.5" ></text>
</g>
<g >
<title>all (1,488,114 us, 100%)</title><rect x="10.0" y="30" width="4076.0" height="23.0" fill="rgb(255,0,0)" rx="2" ry="2" />
<text  x="13.00" y="44.5" ></text>
</g>
<g >
<title>'recursive_function $((depth + 1))' (137 us, 0.01%)</title><rect x="4084.6" y="198" width="0.4" height="23.0" fill="rgb(17,42,251)" rx="2" ry="2" />
<text  x="4087.58" y="212.5" ></text>
</g>
<g >
<title>FUNCTION (recursive_function)_[f] (103,587 us, 6.96%)</title><rect x="3802.3" y="150" width="283.7" height="23.0" fill="rgb(221.665602058209,188.331204116417,236.666081132015)" rx="2" ry="2" />
<text  x="3805.27" y="164.5" >FUNCTION (recursive_function)_[f]</text>
</g>
<g >
<title>FUNCTION (recursive_function)_[f] (520 us, 0.03%)</title><rect x="4084.6" y="174" width="1.4" height="23.0" fill="rgb(207.129113082341,159.258226164683,228.671012195288)" rx="2" ry="2" />
<text  x="4087.58" y="188.5" ></text>
</g>
<g >
<title>'echo &quot;[RECURSIVE] Depth: $depth&quot; 1&gt;&amp;2' (82 us, 0.01%)</title><rect x="3803.1" y="174" width="0.2" height="23.0" fill="rgb(17,42,251)" rx="2" ry="2" />
<text  x="3806.11" y="188.5" ></text>
</g>
<g >
<title>SUBSHELL (964884)_[s] (1,488,114 us, 100.00%)</title><rect x="10.0" y="54" width="4076.0" height="23.0" fill="rgb(236.666666666468,188.333333332613,221.666666666306)" rx="2" ry="2" />
<text  x="13.00" y="68.5" >SUBSHELL (964884)_[s]</text>
</g>
<g >
<title>FUNCTION (recursive_function)_[f] (206,480 us, 13.88%)</title><rect x="3520.4" y="126" width="565.6" height="23.0" fill="rgb(221.666258154097,188.332516308195,236.666441984754)" rx="2" ry="2" />
<text  x="3523.44" y="140.5" >FUNCTION (recursive_function)_[f]</text>
</g>
<g >
<title>'local depth=$1' (145 us, 0.01%)</title><rect x="4085.0" y="198" width="0.3" height="23.0" fill="rgb(17,44,251)" rx="2" ry="2" />
<text  x="4087.95" y="212.5" ></text>
</g>
<g >
<title>'read -r line' (323,813 us, 21.76%)</title><rect x="2354.3" y="102" width="886.9" height="23.0" fill="rgb(211,211,212)" rx="2" ry="2" />
<text  x="2357.28" y="116.5" >'read -r line'</text>
</g>
<g >
<title>'mkfifo &quot;$INPUT_PIPE&quot;' (1,315 us, 0.09%)</title><rect x="1173.5" y="78" width="3.6" height="23.0" fill="rgb(20,48,250)" rx="2" ry="2" />
<text  x="1176.46" y="92.5" ></text>
</g>
<g >
<title>SUBSHELL (964912)_[s] (328,859 us, 22.10%)</title><rect x="2341.4" y="78" width="900.8" height="23.0" fill="rgb(236.666604380397,188.333106837807,221.666553418903)" rx="2" ry="2" />
<text  x="2344.44" y="92.5" >SUBSHELL (964912)_[s]</text>
</g>
<g >
<title>'INPUT_PIPE=&quot;$TMPDIR/input.pipe&quot;' (67 us, 0.00%)</title><rect x="1173.1" y="78" width="0.2" height="23.0" fill="rgb(16,40,251)" rx="2" ry="2" />
<text  x="1176.09" y="92.5" ></text>
</g>
<g >
<title>'local depth=$1' (70 us, 0.00%)</title><rect x="3242.4" y="126" width="0.2" height="23.0" fill="rgb(16,41,251)" rx="2" ry="2" />
<text  x="3245.38" y="140.5" ></text>
</g>
<g >
<title>SUBSHELL (964917)_[s] (308,066 us, 20.70%)</title><rect x="3242.2" y="78" width="843.8" height="23.0" fill="rgb(236.666635609709,188.333220398943,221.666610199471)" rx="2" ry="2" />
<text  x="3245.20" y="92.5" >SUBSHELL (964917)_[s]</text>
</g>
<g >
<title>'echo &quot;$line&quot;' (105 us, 0.01%)</title><rect x="3241.6" y="102" width="0.3" height="23.0" fill="rgb(17,42,251)" rx="2" ry="2" />
<text  x="3244.57" y="116.5" ></text>
</g>
<g >
<title>'echo &quot;[RECURSIVE] Depth: $depth&quot; 1&gt;&amp;2' (77 us, 0.01%)</title><rect x="3242.8" y="126" width="0.2" height="23.0" fill="rgb(17,42,251)" rx="2" ry="2" />
<text  x="3245.77" y="140.5" ></text>
</g>
<g >
<title>'recursive_function $((depth + 1))' (128 us, 0.01%)</title><rect x="3520.4" y="150" width="0.4" height="23.0" fill="rgb(17,43,251)" rx="2" ry="2" />
<text  x="3523.44" y="164.5" ></text>
</g>
<g >
<title>'TMPDIR=$(mktemp -d)' (478 us, 0.03%)</title><rect x="10.2" y="78" width="1.3" height="23.0" fill="rgb(40,88,246)" rx="2" ry="2" />
<text  x="13.18" y="92.5" ></text>
</g>
</g>
</svg>
