<?xml version="1.0" standalone="no"?>
<!DOCTYPE svg PUBLIC "-//W3C//DTD SVG 1.1//EN" "http://www.w3.org/Graphics/SVG/1.1/DTD/svg11.dtd">
<svg version="1.1" width="4096" height="272" onload="init(evt)" viewBox="0 0 4096 272" xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink">
<!-- Flame graph stack visualization. See https://github.com/brendangregg/FlameGraph for latest version, and http://www.brendangregg.com/flamegraphs.html for examples. -->
<!-- NOTES:  -->
<defs>
        <linearGradient id="background" y1="0" y2="1" x1="0" x2="0" >
                <stop stop-color="#f8f8f8" offset="5%" />
                <stop stop-color="#e8e8e8" offset="95%" />
        </linearGradient>
</defs>
<style type="text/css">
        text { font-family:Verdana; font-size:10px; fill:rgb(0,0,0); }
        .hide { display:none; }
        .parent { opacity:0.5; }
</style>
<script type="text/ecmascript">
<![CDATA[
        "use strict";
        var details, searchbtn, unzoombtn, matchedtxt, svg, searching, currentSearchTerm, ignorecase, ignorecaseBtn;
        function init(evt) {
                details = document.getElementById("details").firstChild;
                searchbtn = document.getElementById("search");
                ignorecaseBtn = document.getElementById("ignorecase");
                unzoombtn = document.getElementById("unzoom");
                matchedtxt = document.getElementById("matched");
                svg = document.getElementsByTagName("svg")[0];
                searching = 0;
                currentSearchTerm = null;

                // use GET parameters to restore a flamegraphs state.
                var params = get_params();
                if (params.x && params.y)
                        zoom(find_group(document.querySelector('[x="' + params.x + '"][y="' + params.y + '"]')));
                if (params.s) search(params.s);
        }

        // event listeners
        window.addEventListener("click", function(e) {
                var target = find_group(e.target);
                if (target) {
                        if (target.nodeName == "a") {
                                if (e.ctrlKey === false) return;
                                e.preventDefault();
                        }
                        if (target.classList.contains("parent")) unzoom(true);
                        zoom(target);
                        if (!document.querySelector('.parent')) {
                                // we have basically done a clearzoom so clear the url
                                var params = get_params();
                                if (params.x) delete params.x;
                                if (params.y) delete params.y;
                                history.replaceState(null, null, parse_params(params));
                                unzoombtn.classList.add("hide");
                                return;
                        }

                        // set parameters for zoom state
                        var el = target.querySelector("rect");
                        if (el && el.attributes && el.attributes.y && el.attributes._orig_x) {
                                var params = get_params()
                                params.x = el.attributes._orig_x.value;
                                params.y = el.attributes.y.value;
                                history.replaceState(null, null, parse_params(params));
                        }
                }
                else if (e.target.id == "unzoom") clearzoom();
                else if (e.target.id == "search") search_prompt();
                else if (e.target.id == "ignorecase") toggle_ignorecase();
        }, false)

        // mouse-over for info
        // show
        window.addEventListener("mouseover", function(e) {
                var target = find_group(e.target);
                if (target) details.nodeValue = "Function: " + g_to_text(target);
        }, false)

        // clear
        window.addEventListener("mouseout", function(e) {
                var target = find_group(e.target);
                if (target) details.nodeValue = ' ';
        }, false)

        // ctrl-F for search
        // ctrl-I to toggle case-sensitive search
        window.addEventListener("keydown",function (e) {
                if (e.keyCode === 114 || (e.ctrlKey && e.keyCode === 70)) {
                        e.preventDefault();
                        search_prompt();
                }
                else if (e.ctrlKey && e.keyCode === 73) {
                        e.preventDefault();
                        toggle_ignorecase();
                }
        }, false)

        // functions
        function get_params() {
                var params = {};
                var paramsarr = window.location.search.substr(1).split('&');
                for (var i = 0; i < paramsarr.length; ++i) {
                        var tmp = paramsarr[i].split("=");
                        if (!tmp[0] || !tmp[1]) continue;
                        params[tmp[0]]  = decodeURIComponent(tmp[1]);
                }
                return params;
        }
        function parse_params(params) {
                var uri = "?";
                for (var key in params) {
                        uri += key + '=' + encodeURIComponent(params[key]) + '&';
                }
                if (uri.slice(-1) == "&")
                        uri = uri.substring(0, uri.length - 1);
                if (uri == '?')
                        uri = window.location.href.split('?')[0];
                return uri;
        }
        function find_child(node, selector) {
                var children = node.querySelectorAll(selector);
                if (children.length) return children[0];
        }
        function find_group(node) {
                var parent = node.parentElement;
                if (!parent) return;
                if (parent.id == "frames") return node;
                return find_group(parent);
        }
        function orig_save(e, attr, val) {
                if (e.attributes["_orig_" + attr] != undefined) return;
                if (e.attributes[attr] == undefined) return;
                if (val == undefined) val = e.attributes[attr].value;
                e.setAttribute("_orig_" + attr, val);
        }
        function orig_load(e, attr) {
                if (e.attributes["_orig_"+attr] == undefined) return;
                e.attributes[attr].value = e.attributes["_orig_" + attr].value;
                e.removeAttribute("_orig_"+attr);
        }
        function g_to_text(e) {
                var text = find_child(e, "title").firstChild.nodeValue;
                return (text)
        }
        function g_to_func(e) {
                var func = g_to_text(e);
                // if there's any manipulation we want to do to the function
                // name before it's searched, do it here before returning.
                return (func);
        }
        function update_text(e) {
                var r = find_child(e, "rect");
                var t = find_child(e, "text");
                var w = parseFloat(r.attributes.width.value) -3;
                var txt = find_child(e, "title").textContent.replace(/\([^(]*\)$/,"");
                t.attributes.x.value = parseFloat(r.attributes.x.value) + 3;

                // Smaller than this size won't fit anything
                if (w < 2 * 10 * 0.59) {
                        t.textContent = "";
                        return;
                }

                t.textContent = txt;
                var sl = t.getSubStringLength(0, txt.length);
                // check if only whitespace or if we can fit the entire string into width w
                if (/^ *$/.test(txt) || sl < w)
                        return;

                // this isn't perfect, but gives a good starting point
                // and avoids calling getSubStringLength too often
                var start = Math.floor((w/sl) * txt.length);
                for (var x = start; x > 0; x = x-2) {
                        if (t.getSubStringLength(0, x + 2) <= w) {
                                t.textContent = txt.substring(0, x) + "..";
                                return;
                        }
                }
                t.textContent = "";
        }

        // zoom
        function zoom_reset(e) {
                if (e.attributes != undefined) {
                        orig_load(e, "x");
                        orig_load(e, "width");
                }
                if (e.childNodes == undefined) return;
                for (var i = 0, c = e.childNodes; i < c.length; i++) {
                        zoom_reset(c[i]);
                }
        }
        function zoom_child(e, x, ratio) {
                if (e.attributes != undefined) {
                        if (e.attributes.x != undefined) {
                                orig_save(e, "x");
                                e.attributes.x.value = (parseFloat(e.attributes.x.value) - x - 10) * ratio + 10;
                                if (e.tagName == "text")
                                        e.attributes.x.value = find_child(e.parentNode, "rect[x]").attributes.x.value + 3;
                        }
                        if (e.attributes.width != undefined) {
                                orig_save(e, "width");
                                e.attributes.width.value = parseFloat(e.attributes.width.value) * ratio;
                        }
                }

                if (e.childNodes == undefined) return;
                for (var i = 0, c = e.childNodes; i < c.length; i++) {
                        zoom_child(c[i], x - 10, ratio);
                }
        }
        function zoom_parent(e) {
                if (e.attributes) {
                        if (e.attributes.x != undefined) {
                                orig_save(e, "x");
                                e.attributes.x.value = 10;
                        }
                        if (e.attributes.width != undefined) {
                                orig_save(e, "width");
                                e.attributes.width.value = parseInt(svg.width.baseVal.value) - (10 * 2);
                        }
                }
                if (e.childNodes == undefined) return;
                for (var i = 0, c = e.childNodes; i < c.length; i++) {
                        zoom_parent(c[i]);
                }
        }
        function zoom(node) {
                var attr = find_child(node, "rect").attributes;
                var width = parseFloat(attr.width.value);
                var xmin = parseFloat(attr.x.value);
                var xmax = parseFloat(xmin + width);
                var ymin = parseFloat(attr.y.value);
                var ratio = (svg.width.baseVal.value - 2 * 10) / width;

                // XXX: Workaround for JavaScript float issues (fix me)
                var fudge = 0.0001;

                unzoombtn.classList.remove("hide");

                var el = document.getElementById("frames").children;
                for (var i = 0; i < el.length; i++) {
                        var e = el[i];
                        var a = find_child(e, "rect").attributes;
                        var ex = parseFloat(a.x.value);
                        var ew = parseFloat(a.width.value);
                        var upstack;
                        // Is it an ancestor
                        if (0 == 0) {
                                upstack = parseFloat(a.y.value) > ymin;
                        } else {
                                upstack = parseFloat(a.y.value) < ymin;
                        }
                        if (upstack) {
                                // Direct ancestor
                                if (ex <= xmin && (ex+ew+fudge) >= xmax) {
                                        e.classList.add("parent");
                                        zoom_parent(e);
                                        update_text(e);
                                }
                                // not in current path
                                else
                                        e.classList.add("hide");
                        }
                        // Children maybe
                        else {
                                // no common path
                                if (ex < xmin || ex + fudge >= xmax) {
                                        e.classList.add("hide");
                                }
                                else {
                                        zoom_child(e, xmin, ratio);
                                        update_text(e);
                                }
                        }
                }
                search();
        }
        function unzoom(dont_update_text) {
                unzoombtn.classList.add("hide");
                var el = document.getElementById("frames").children;
                for(var i = 0; i < el.length; i++) {
                        el[i].classList.remove("parent");
                        el[i].classList.remove("hide");
                        zoom_reset(el[i]);
                        if(!dont_update_text) update_text(el[i]);
                }
                search();
        }
        function clearzoom() {
                unzoom();

                // remove zoom state
                var params = get_params();
                if (params.x) delete params.x;
                if (params.y) delete params.y;
                history.replaceState(null, null, parse_params(params));
        }

        // search
        function toggle_ignorecase() {
                ignorecase = !ignorecase;
                if (ignorecase) {
                        ignorecaseBtn.classList.add("show");
                } else {
                        ignorecaseBtn.classList.remove("show");
                }
                reset_search();
                search();
        }
        function reset_search() {
                var el = document.querySelectorAll("#frames rect");
                for (var i = 0; i < el.length; i++) {
                        orig_load(el[i], "fill")
                }
                var params = get_params();
                delete params.s;
                history.replaceState(null, null, parse_params(params));
        }
        function search_prompt() {
                if (!searching) {
                        var term = prompt("Enter a search term (regexp " +
                            "allowed, eg: ^ext4_)"
                            + (ignorecase ? ", ignoring case" : "")
                            + "\nPress Ctrl-i to toggle case sensitivity", "");
                        if (term != null) search(term);
                } else {
                        reset_search();
                        searching = 0;
                        currentSearchTerm = null;
                        searchbtn.classList.remove("show");
                        searchbtn.firstChild.nodeValue = "Search"
                        matchedtxt.classList.add("hide");
                        matchedtxt.firstChild.nodeValue = ""
                }
        }
        function search(term) {
                if (term) currentSearchTerm = term;
                if (currentSearchTerm === null) return;

                var re = new RegExp(currentSearchTerm, ignorecase ? 'i' : '');
                var el = document.getElementById("frames").children;
                var matches = new Object();
                var maxwidth = 0;
                for (var i = 0; i < el.length; i++) {
                        var e = el[i];
                        var func = g_to_func(e);
                        var rect = find_child(e, "rect");
                        if (func == null || rect == null)
                                continue;

                        // Save max width. Only works as we have a root frame
                        var w = parseFloat(rect.attributes.width.value);
                        if (w > maxwidth)
                                maxwidth = w;

                        if (func.match(re)) {
                                // highlight
                                var x = parseFloat(rect.attributes.x.value);
                                orig_save(rect, "fill");
                                rect.attributes.fill.value = "rgb(230,0,230)";

                                // remember matches
                                if (matches[x] == undefined) {
                                        matches[x] = w;
                                } else {
                                        if (w > matches[x]) {
                                                // overwrite with parent
                                                matches[x] = w;
                                        }
                                }
                                searching = 1;
                        }
                }
                if (!searching)
                        return;
                var params = get_params();
                params.s = currentSearchTerm;
                history.replaceState(null, null, parse_params(params));

                searchbtn.classList.add("show");
                searchbtn.firstChild.nodeValue = "Reset Search";

                // calculate percent matched, excluding vertical overlap
                var count = 0;
                var lastx = -1;
                var lastw = 0;
                var keys = Array();
                for (k in matches) {
                        if (matches.hasOwnProperty(k))
                                keys.push(k);
                }
                // sort the matched frames by their x location
                // ascending, then width descending
                keys.sort(function(a, b){
                        return a - b;
                });
                // Step through frames saving only the biggest bottom-up frames
                // thanks to the sort order. This relies on the tree property
                // where children are always smaller than their parents.
                var fudge = 0.0001;     // JavaScript floating point
                for (var k in keys) {
                        var x = parseFloat(keys[k]);
                        var w = matches[keys[k]];
                        if (x >= lastx + lastw - fudge) {
                                count += w;
                                lastx = x;
                                lastw = w;
                        }
                }
                // display matched percent
                matchedtxt.classList.remove("hide");
                var pct = 100 * count / maxwidth;
                if (pct != 100) pct = pct.toFixed(1)
                matchedtxt.firstChild.nodeValue = "Matched: " + pct + "%";
        }
]]>
</script>
<rect x="0.0" y="0" width="4096.0" height="272.0" fill="url(#background)"  />
<text id="title" x="2048.00" y="20" >FlameGraph: /mnt/ramdisk/timep/TESTS/gemini-2.5/gemini-2.5.test.bash</text>
<text id="details" x="10.00" y="257" > </text>
<text id="unzoom" x="10.00" y="20" class="hide">Reset Zoom</text>
<text id="search" x="3986.00" y="20" >Search</text>
<text id="ignorecase" x="4070.00" y="20" >ic</text>
<text id="matched" x="3986.00" y="257" > </text>
<g id="frames">
<g >
<title>'set -o monitor' (68 us, 0.00%)</title><rect x="10.0" y="171" width="0.2" height="23.0" fill="rgb(17,42,251)" rx="2" ry="2" />
<text  x="13.00" y="185.5" ></text>
</g>
<g >
<title>'echo &quot;[RECURSIVE] Depth: $depth&quot; 1&gt;&amp;2' (79 us, 0.01%)</title><rect x="3255.5" y="123" width="0.3" height="23.0" fill="rgb(17,43,251)" rx="2" ry="2" />
<text  x="3258.54" y="137.5" ></text>
</g>
<g >
<title>'local depth=$1' (73 us, 0.00%)</title><rect x="4085.4" y="51" width="0.2" height="23.0" fill="rgb(16,41,251)" rx="2" ry="2" />
<text  x="4088.41" y="65.5" ></text>
</g>
<g >
<title>'recursive_function 1' (67 us, 0.00%)</title><rect x="3255.0" y="123" width="0.1" height="23.0" fill="rgb(16,41,251)" rx="2" ry="2" />
<text  x="3257.96" y="137.5" ></text>
</g>
<g >
<title>'TMPDIR=$(mktemp -d)' (475 us, 0.03%)</title><rect x="10.2" y="171" width="1.3" height="23.0" fill="rgb(39,87,246)" rx="2" ry="2" />
<text  x="13.18" y="185.5" ></text>
</g>
<g >
<title>all (1,511,924 us, 100%)</title><rect x="10.0" y="219" width="4076.0" height="23.0" fill="rgb(255,0,0)" rx="2" ry="2" />
<text  x="13.00" y="233.5" ></text>
</g>
<g >
<title>'local depth=$1' (71 us, 0.00%)</title><rect x="3807.6" y="75" width="0.2" height="23.0" fill="rgb(16,42,251)" rx="2" ry="2" />
<text  x="3810.59" y="89.5" ></text>
</g>
<g >
<title>'sleep 0.1' (101,562 us, 6.72%)</title><rect x="3255.8" y="123" width="273.8" height="23.0" fill="rgb(210,211,212)" rx="2" ry="2" />
<text  x="3258.75" y="137.5" >'sleep 0.1'</text>
</g>
<g >
<title>'[ $depth -gt 3 ]' (71 us, 0.00%)</title><rect x="3255.3" y="123" width="0.2" height="23.0" fill="rgb(16,42,251)" rx="2" ry="2" />
<text  x="3258.35" y="137.5" ></text>
</g>
<g >
<title>'[ $depth -gt 3 ]' (71 us, 0.00%)</title><rect x="3807.8" y="75" width="0.2" height="23.0" fill="rgb(16,41,251)" rx="2" ry="2" />
<text  x="3810.79" y="89.5" ></text>
</g>
<g >
<title>'echo &quot;$line&quot;' (73 us, 0.00%)</title><rect x="2374.8" y="147" width="0.2" height="23.0" fill="rgb(17,42,251)" rx="2" ry="2" />
<text  x="2377.79" y="161.5" ></text>
</g>
<g >
<title>FUNCTION (recursive_function)_[f] (290 us, 0.02%)</title><rect x="4085.2" y="75" width="0.8" height="23.0" fill="rgb(207.664842877892,160.329685755784,228.965663582841)" rx="2" ry="2" />
<text  x="4088.22" y="89.5" ></text>
</g>
<g >
<title>SUBSHELL (1314587)_[s] (308,261 us, 20.39%)</title><rect x="3255.0" y="171" width="831.0" height="23.0" fill="rgb(236.666637126933,188.333225916121,221.66661295806)" rx="2" ry="2" />
<text  x="3257.96" y="185.5" >SUBSHELL (1314587)_[s]</text>
</g>
<g >
<title>SUBSHELL (1314554)_[s] (1,511,924 us, 100.00%)</title><rect x="10.0" y="195" width="4076.0" height="23.0" fill="rgb(236.666665437389,188.333328863233,221.666664431616)" rx="2" ry="2" />
<text  x="13.00" y="209.5" >SUBSHELL (1314554)_[s]</text>
</g>
<g >
<title>SUBSHELL (1314579)_[s] (436,121 us, 28.85%)</title><rect x="1193.6" y="171" width="1175.7" height="23.0" fill="rgb(251.098685432708,155.795143860297,226.018806071548)" rx="2" ry="2" />
<text  x="1196.55" y="185.5" >SUBSHELL (1314579)_[s]</text>
</g>
<g >
<title>'echo &quot;[RECURSIVE] Depth: $depth&quot; 1&gt;&amp;2' (95 us, 0.01%)</title><rect x="3808.0" y="75" width="0.2" height="23.0" fill="rgb(17,44,251)" rx="2" ry="2" />
<text  x="3810.98" y="89.5" ></text>
</g>
<g >
<title>'read -r line' (1,936 us, 0.13%)</title><rect x="2369.3" y="147" width="5.2" height="23.0" fill="rgb(144,195,225)" rx="2" ry="2" />
<text  x="2372.29" y="161.5" ></text>
</g>
<g >
<title>'read -r line' (116 us, 0.01%)</title><rect x="3254.6" y="147" width="0.4" height="23.0" fill="rgb(17,43,251)" rx="2" ry="2" />
<text  x="3257.65" y="161.5" ></text>
</g>
<g >
<title>'OUTPUT_PIPE=&quot;$TMPDIR/output.pipe&quot;' (68 us, 0.00%)</title><rect x="1187.4" y="171" width="0.1" height="23.0" fill="rgb(16,41,251)" rx="2" ry="2" />
<text  x="1190.35" y="185.5" ></text>
</g>
<g >
<title>'[ $depth -gt 3 ]' (70 us, 0.00%)</title><rect x="3529.9" y="99" width="0.2" height="23.0" fill="rgb(16,42,251)" rx="2" ry="2" />
<text  x="3532.92" y="113.5" ></text>
</g>
<g >
<title>'mkfifo &quot;$OUTPUT_PIPE&quot;' (1,078 us, 0.07%)</title><rect x="1190.6" y="171" width="3.0" height="23.0" fill="rgb(19,48,250)" rx="2" ry="2" />
<text  x="1193.65" y="185.5" ></text>
</g>
<g >
<title>'mkfifo &quot;$INPUT_PIPE&quot;' (1,153 us, 0.08%)</title><rect x="1187.5" y="171" width="3.1" height="23.0" fill="rgb(20,49,250)" rx="2" ry="2" />
<text  x="1190.54" y="185.5" ></text>
</g>
<g >
<title>'echo &quot;[COPROC] Received: $line&quot; 1&gt;&amp;2' (117 us, 0.01%)</title><rect x="3254.0" y="147" width="0.4" height="23.0" fill="rgb(16,40,251)" rx="2" ry="2" />
<text  x="3257.05" y="161.5" ></text>
</g>
<g >
<title>'recursive_function $((depth + 1))' (71 us, 0.00%)</title><rect x="3807.4" y="75" width="0.2" height="23.0" fill="rgb(16,41,251)" rx="2" ry="2" />
<text  x="3810.40" y="89.5" ></text>
</g>
<g >
<title>FUNCTION (recursive_function)_[f] (206,405 us, 13.65%)</title><rect x="3529.6" y="123" width="556.4" height="23.0" fill="rgb(221.666546950879,188.333093901757,236.666600822983)" rx="2" ry="2" />
<text  x="3532.55" y="137.5" >FUNCTION (recursive_function)_[f]</text>
</g>
<g >
<title>'sleep 0.1' (102,776 us, 6.80%)</title><rect x="3530.3" y="99" width="277.1" height="23.0" fill="rgb(207,211,212)" rx="2" ry="2" />
<text  x="3533.33" y="113.5" >'sleep 0.1'</text>
</g>
<g >
<title>'INPUT_PIPE=&quot;$TMPDIR/input.pipe&quot;' (71 us, 0.00%)</title><rect x="1187.2" y="171" width="0.2" height="23.0" fill="rgb(16,42,251)" rx="2" ry="2" />
<text  x="1190.16" y="185.5" ></text>
</g>
<g >
<title>'echo &quot;$line&quot;' (104 us, 0.01%)</title><rect x="3254.4" y="147" width="0.2" height="23.0" fill="rgb(17,42,251)" rx="2" ry="2" />
<text  x="3257.37" y="161.5" ></text>
</g>
<g >
<title>'@TRAP (SIGCHLD): echo &quot;[TRAP] A child process has exited.&quot; :: @TRAP (SIGCHLD): echo &quot;[TRAP] A child process has exited.&quot; :: @TRAP (SIGCHLD): echo &quot;[TRAP] A child process has exited.&quot; :: @TRAP (SIGCHLD): echo &quot;[TRAP] A child process has exited.&quot; :: @TRAP (S' (436,106 us, 28.84%)</title><rect x="11.5" y="171" width="1175.7" height="23.0" fill="rgb(148,206,116)" rx="2" ry="2" />
<text  x="14.46" y="185.5" >'@TRAP (SIGCHLD): echo "[TRAP] A child process has exited." :: @TRAP (SIGCHLD): echo "[TRAP] A child process has exited." :: @TRAP (SIGCHLD): echo "[TRAP] A child process has exited." :: @TRAP (SIG..</text>
</g>
<g >
<title>'echo &quot;[COPROC] Received: $line&quot; 1&gt;&amp;2' (104 us, 0.01%)</title><rect x="2374.5" y="147" width="0.3" height="23.0" fill="rgb(19,47,251)" rx="2" ry="2" />
<text  x="2377.51" y="161.5" ></text>
</g>
<g >
<title>'local depth=$1' (70 us, 0.00%)</title><rect x="3529.7" y="99" width="0.2" height="23.0" fill="rgb(16,42,251)" rx="2" ry="2" />
<text  x="3532.73" y="113.5" ></text>
</g>
<g >
<title>SUBSHELL (1314582)_[s] (328,523 us, 21.73%)</title><rect x="2369.3" y="171" width="885.7" height="23.0" fill="rgb(236.666287050196,188.331952909802,221.665976454901)" rx="2" ry="2" />
<text  x="2372.29" y="185.5" >SUBSHELL (1314582)_[s]</text>
</g>
<g >
<title>'local depth=$1' (77 us, 0.01%)</title><rect x="3255.1" y="123" width="0.2" height="23.0" fill="rgb(18,45,251)" rx="2" ry="2" />
<text  x="3258.14" y="137.5" ></text>
</g>
<g >
<title>'[ $depth -gt 3 ]' (72 us, 0.00%)</title><rect x="4085.6" y="51" width="0.2" height="23.0" fill="rgb(16,41,251)" rx="2" ry="2" />
<text  x="4088.61" y="65.5" ></text>
</g>
<g >
<title>'read -r line' (326,073 us, 21.57%)</title><rect x="2375.0" y="147" width="879.0" height="23.0" fill="rgb(211,211,212)" rx="2" ry="2" />
<text  x="2377.99" y="161.5" >'read -r line'</text>
</g>
<g >
<title>'recursive_function $((depth + 1))' (66 us, 0.00%)</title><rect x="3529.6" y="99" width="0.1" height="23.0" fill="rgb(16,40,251)" rx="2" ry="2" />
<text  x="3532.55" y="113.5" ></text>
</g>
<g >
<title>FUNCTION (recursive_function)_[f] (103,341 us, 6.84%)</title><rect x="3807.4" y="99" width="278.6" height="23.0" fill="rgb(221.66612952199,188.332259043979,236.666371237094)" rx="2" ry="2" />
<text  x="3810.40" y="113.5" >FUNCTION (recursive_function)_[f]</text>
</g>
<g >
<title>'echo &quot;[RECURSIVE] Depth: $depth&quot; 1&gt;&amp;2' (82 us, 0.01%)</title><rect x="3530.1" y="99" width="0.2" height="23.0" fill="rgb(17,43,251)" rx="2" ry="2" />
<text  x="3533.11" y="113.5" ></text>
</g>
<g >
<title>'recursive_function $((depth + 1))' (72 us, 0.00%)</title><rect x="4085.2" y="51" width="0.2" height="23.0" fill="rgb(17,42,251)" rx="2" ry="2" />
<text  x="4088.22" y="65.5" ></text>
</g>
<g >
<title>'mktemp -d' (436,121 us, 28.85%)</title><rect x="1193.6" y="147" width="1175.7" height="23.0" fill="rgb(230,71,23)" rx="2" ry="2" />
<text  x="1196.55" y="161.5" >'mktemp -d'</text>
</g>
<g >
<title>'return' (73 us, 0.00%)</title><rect x="4085.8" y="51" width="0.2" height="23.0" fill="rgb(16,41,251)" rx="2" ry="2" />
<text  x="4088.80" y="65.5" ></text>
</g>
<g >
<title>FUNCTION (recursive_function)_[f] (308,261 us, 20.39%)</title><rect x="3255.0" y="147" width="831.0" height="23.0" fill="rgb(221.66661295806,188.333225916121,236.666637126933)" rx="2" ry="2" />
<text  x="3257.96" y="161.5" >FUNCTION (recursive_function)_[f]</text>
</g>
<g >
<title>'sleep 0.1' (102,743 us, 6.80%)</title><rect x="3808.2" y="75" width="277.0" height="23.0" fill="rgb(208,211,212)" rx="2" ry="2" />
<text  x="3811.23" y="89.5" >'sleep 0.1'</text>
</g>
</g>
</svg>
